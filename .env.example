# Nơi nhận đơn (URL Web App của Google Apps Script, xem backend/apps-script/README.md).
# Để trống: đơn chỉ lưu trên máy khách (chế độ chạy thử).
VITE_ORDER_ENDPOINT=
# Tùy chọn, phải trùng CONFIG.SHARED_KEY trong Code.gs. Không phải bí mật thật (nằm trong code web), chỉ chặn bớt spam.
VITE_ORDER_KEY=
