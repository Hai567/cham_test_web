'use strict';
// Stub for node-canvas (see package.json description). Throwing on require makes
// optional consumers such as jsdom fall back exactly as if canvas were not installed.
throw new Error('node-canvas is stubbed in this project on purpose.');
